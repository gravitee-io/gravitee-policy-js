{
    "id": "api-request-properties",
    "name": "api-request-properties",
    "apiVersion": "1.0",
    "definitionVersion": "4.0.0",
    "type": "proxy",
    "listeners": [
        {
            "type": "http",
            "paths": [{ "path": "/test" }],
            "entrypoints": [{ "type": "http-proxy" }]
        }
    ],
    "endpointGroups": [
        {
            "name": "default",
            "type": "http-proxy",
            "endpoints": [
                {
                    "name": "backend",
                    "type": "http-proxy",
                    "configuration": { "target": "http://localhost:8080/foobar" }
                }
            ]
        }
    ],
    "flows": [
        {
            "name": "flow-1",
            "enabled": true,
            "selectors": [{ "type": "http", "path": "/", "pathOperator": "STARTS_WITH" }],
            "request": [
                {
                    "name": "JS",
                    "enabled": true,
                    "policy": "js",
                    "configuration": {
                        "script": "request.headers().set('X-Id', request.id()).set('X-Path', request.path()).set('X-ContextPath', request.contextPath()).set('X-Method', request.method()).set('X-Scheme', request.scheme()).set('X-Host', request.host()).set('X-Timestamp', String(request.timestamp())).set('X-Version', request.version());"
                    }
                }
            ],
            "response": []
        }
    ]
}
